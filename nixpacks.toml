# Nixpacks configuration for EMLESS on Railway
[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install --omit=dev"]

[phases.build]
cmds = ["echo 'Build complete'"]

[start]
cmd = "node server.js"
